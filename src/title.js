import React from "react";
const Title = (props) =>{
  
  return(
    <div><h1>Weather Playlist with Spotify</h1>
      <p>This app generates a playlist according to your music taste and
         the current weather conditions at your location.
      </p>
    </div>
  )
}
export default Title;